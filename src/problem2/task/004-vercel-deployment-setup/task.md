# Task

## Goal

Prepare Vercel and GitHub access/configuration for later Git-connected deployment.

## Scope

Setup only, without publishing or deployment-triggering pushes. Depends on task 001 for build scripts and local ignore rules; task 006 performs publishing after task 005 verification.

Read `.ai/ai_workflow.md`, `.ai/coding_convention.md`, `requirement.md`, and `plans/001-currency-swap.md` before execution. Work within the current `problem2` project, preserve user changes, and use the workflow's status/execution recording rules.

## Target Files

`README.md`, project-local `.gitignore`, ignored `.vercel/project.json` generated by CLI; Vercel project settings. Add `vercel.json` only if defaults are insufficient. No modification of unrelated project files.

## Steps

1. Verify Vercel identity and access to team coding-challenge. Intended project is currency-swap; inspect it first and reuse the intended project or create it if absent.
2. Use existing GitHub repository https://github.com/nguyendichtu91295/code-challenge.git and production branch main. Inspect remote identity and check authenticated push permissions using available read-only provider tooling; a read-only fetch alone does not prove write permission.
3. Check Vercel's GitHub integration can access the repository. If missing, complete the required browser authorization. If access cannot be established, record the exact unmet check rather than claiming readiness.
4. Link the app to coding-challenge/currency-swap with explicit team scope. Use CLI help/API documentation for current syntax. Resolve CLI working-directory behavior against the repository root src/problem2 to avoid double-prefixing paths.
5. Set framework Vite, repository root src/problem2, build npm run build, output dist and production branch main. Inspect any existing configuration before changing it.
6. Keep .vercel metadata ignored. No app secrets, custom domain, backend or new Git repository are needed.
7. Document later vercel git connect and push workflow. Defer actual Git connection if it can trigger deployment, commits, pushes, and publishing until task 006.

## Validation

- `vercel whoami` and `vercel teams ls` — authenticated user can access coding-challenge.
- `vercel project inspect currency-swap --scope coding-challenge` plus read-only project/API and local-link inspection — project identity, root/build/output and production branch match the intended values.
- `git check-ignore .vercel/project.json` — generated metadata is ignored.
- Record evidence for GitHub write permission and Vercel repository access; do not expose tokens. Explicitly identify any access check that requires final connection to confirm.
- `npm run build` if setup changes build configuration; otherwise reuse the prior successful build and record that no app source changed.
